/*
 * Root Messages
 *
 * This contains all the text for the Root component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  loaderText: {
    id: 'app.containers.root.loaderText',
    defaultMessage: 'Hang tight.',
  },
});
